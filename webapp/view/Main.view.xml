<mvc:View
    xmlns:core="sap.ui.core"
    controllerName="project1.controller.Main"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:app="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1"
>
    <Page>
        <IconTabBar
            id="textBar"
            class="sapUiResponsivePaddings"
            select=".onTabPress"
            selectedKey="{viewModel>/selectedTab}"
        >
            <items>
                <IconTabFilter text="{i18n>jsonModel}" key="JSONModel">
                    <Table
                        id="bookTable"
                        mode="MultiSelect"
                        items="{
                            path: 'bookData>/books'
                        }"
                    >
                        <headerToolbar>
                            <Toolbar>
                                <Title text="{i18n>bookList}" />
                                <Button
                                    text="{i18n>addRecord}"
                                    press=".onOpenAddRecordDialog"
                                />
                                <Button
                                    text="{i18n>deleteRecord}"
                                    press=".onOpenDeleteDialog"
                                />

                                <Input
                                    id="searchInput"
                                    width="40%"
                                />

                                <Select
                                    id="genreSelect"
                                    items="{
                            path: 'gernes>/bookGenres'
                        }"
                                    width="10%"
                                >
                                    <core:Item
                                        text="{gernes>key}"
                                        key="{gernes>text}"
                                    />
                                </Select>
                                <Button
                                    text="{i18n>filter}"
                                    press=".onApplyFilters"
                                />
                            </Toolbar>
                        </headerToolbar>
                        <columns>
                            <Column>
                                <Text text="{i18n>nameColumn}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>authorColumn}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>genreColumn}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>releaseDateColumn}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>availableColumn}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>actionColumn}" />
                            </Column>
                        </columns>
                        <items>
                            <ColumnListItem>
                                <cells>
                                    <HBox>
                                        <Text
                                            text="{bookData>name}"
                                            visible="{= ${viewModel>/titleEdit/id} !== ${bookData>id} ? true : ${viewModel>/titleEdit/isVisible} ? false : true }"
                                        />
                                        <Input
                                            value="{bookData>name}"
                                            visible="{= ${viewModel>/titleEdit/isVisible} &amp;&amp; ${viewModel>/titleEdit/id} === ${bookData>id} }"
                                        />
                                    </HBox>
                                    <HBox>
                                        <Text text="{bookData>author}" />
                                    </HBox>
                                    <HBox>
                                        <Text text="{bookData>genre}" />
                                    </HBox>
                                    <HBox>
                                        <Text
                                            core:require="{
                                Formatter: 'project1/model/formatter'
                            }"
                                            text="{
                                path: 'bookData>releasedate',
                                formatter: 'Formatter.formatDate.bind($controller)'
                            }"
                                        />
                                    </HBox>
                                    <HBox>
                                        <Text
                                            text="{bookData>availablequantity}"
                                        />
                                    </HBox>
                                    <HBox>
                                        <Button
                                            text="{= ${viewModel>/titleEdit/isVisible} ? ${viewModel>/titleEdit/id} === ${bookData>id} ? 'Save Title' : 'Edit Title'  : 'Edit Title'}"
                                            press=".onEditTitle"
                                        />
                                    </HBox>
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>
                </IconTabFilter>
                <IconTabFilter text="{i18n>odataV2}" key="V2Model">
                    <Table
                        id="productTable"
                        mode="MultiSelect"
                        items="{
                        path: 'ODataV2>/Products',
                        parameters: {
                            expand: 'Supplier'
                        }
                    }"
                    >
                        <headerToolbar>
                            <Toolbar>
                                <Button
                                    text="{i18n>addRecord}"
                                    press=".onOpenAddV2RecordFragment"
                                />
                                <Button
                                    text="{i18n>deleteRecord}"
                                    press=".onDeleteV2Record"
                                />
                                <ToolbarSeparator />
                                <Input
                                    id="searchField"
                                    liveChange=".onInputSearch"
                                    placeholder="{i18n>searchFieldPlaceholder}"
                                    width="60%"
                                />
                                <ToolbarSeparator />

                                <ComboBox
                                    id="sorterSelection"
                                    selectionChange=".onSorterSelect"
                                    placeholder="Please select column"
                                >
                                    <items>
                                        <core:Item
                                            key="Name"
                                            id="Name"
                                            text="{i18n>products}"
                                        />
                                        <core:Item
                                            key="description"
                                            id="description"
                                            text="{i18n>description}"
                                        />
                                        <core:Item
                                            key="releaseDate"
                                            id="releaseDate"
                                            text="{i18n>releaseDate}"
                                        />
                                        <core:Item
                                            key="discontiniueDate"
                                            id="discontinuedDate"
                                            text="{i18n>discontinuedDate}"
                                        />
                                        <core:Item
                                            key="rating"
                                            id="rating"
                                            text="{i18n>rating}"
                                        />
                                        <core:Item
                                            key="price"
                                            id="price"
                                            text="{i18n>price}"
                                        />
                                    </items>
                                </ComboBox>
                            </Toolbar>
                        </headerToolbar>
                        <columns>
                            <Column>
                                <Text text="{i18n>products}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>description}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>releaseDate}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>discontinuedDate}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>rating}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>price}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>actionColumn}" />
                            </Column>
                        </columns>
                        <items>
                            <ColumnListItem>
                                <HBox>
                                    <Text text="{ODataV2>Name}" />
                                </HBox>
                                <HBox>
                                    <Text text="{ODataV2>Description}" />
                                </HBox>
                                <HBox>
                                    <Text text="{ODataV2>ReleaseDate}" />
                                </HBox>
                                <HBox>
                                    <Text text="{ODataV2>DiscontinuedDate}" />
                                </HBox>
                                <HBox>
                                    <Text text="{ODataV2>Rating}" />
                                </HBox>
                                <HBox>
                                    <Text text="{ODataV2>Price}" />
                                </HBox>
                                <HBox>
                                    <Button
                                        text="edit"
                                        press=".onOpenEditV2Record"
                                        app:recordId="{ODataV2>ID}"
                                    />
                                </HBox>
                            </ColumnListItem>
                        </items>
                    </Table>
                </IconTabFilter>
                <IconTabFilter text="{i18n>odataV4}" key="V4Model" >
                    <Table
                        id="V4dataTable"
                        mode="MultiSelect"
                        items="{
                            path: 'ODataV4>/Products',
                            parameters: {
                                $count: true,
                                $expand: 'Supplier'
                            }
                        }"
                    >
                        <columns>
                            <Column>
                                <Text text="{i18n>id}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>name}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>description}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>releaseDate}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>discontinuedDate}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>rating}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>price}" />
                            </Column>
                        </columns>
                        <items>
                            <ColumnListItem>
                                <HBox>
                                    <Text text="{ODataV4>ID}" />
                                </HBox>
                                <HBox>
                                    <Text text="{ODataV4>Name}" />
                                </HBox>
                                <HBox>
                                    <Text text="{ODataV4>Description}" />
                                </HBox>
                                <HBox>
                                    <Text text="{ODataV4>ReleaseDate}" />
                                </HBox>
                                <HBox>
                                    <Text text="{ODataV4>DiscontinuedDate}" />
                                </HBox>
                                <HBox>
                                    <Text text="{ODataV4>Rating}" />
                                </HBox>
                                <HBox>
                                    <Text text="{ODataV4>Price}" />
                                </HBox>
                            </ColumnListItem>
                        </items>
                    </Table>
                </IconTabFilter>
            </items>
        </IconTabBar>
    </Page>
</mvc:View>
