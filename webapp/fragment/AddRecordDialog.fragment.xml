<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:app="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1"
>
    <Dialog
        id="addRecordDialog"
        title="{i18n>addNewBookRecord}"
        contentWidth="30%"
    >
        <HBox
        direction="Column"
        class="sapUiSmallMargin"
        >
            <Label
                labelFor="bookName"
                text="{i18n>enterBookName}"
            />
            <Input
                id="bookName"
                type="Text"
                value="{viewModel>/newObject/name}"
                required="true"
                change=".onEnterInputValueChange"
                valueStateText="{i18n>bookNameInputValueState}"
            />
            <Label
                labelFor="bookAuthor"
                text="{i18n>enterAuthorName}"
            />
            <Input
                id="bookAuthor"
                type="Text"
                value="{viewModel>/newObject/author}"
                required="true"
                change=".onEnterInputValueChange"
                valueStateText="{i18n>bookAuthorInputValueState}"
            />
            <Label
                labelFor="bookGenre"
                text="{i18n>enterGenre}"
            />
            <Input
                id="bookGenre"
                type="Text"
                value="{viewModel>/newObject/genre}"
                required="true"
                change=".onEnterInputValueChange"
                valueStateText="{i18n>bookGenreInputValueState}"
            />
            <Label
                labelFor="bookReleaseDate"
                text="{i18n>enterReleaseDate}"
            />
            <DatePicker
                id="bookReleaseDate"
                value="{viewModel>/newObject/releasedate}"
                required="true"
                change=".onEnterInputValueChange"
                valueStateText="{i18n>bookeReleaseDateValueState}"
            />
            <Label
                labelFor="bookAvailableQuantity"
                text="{i18n>enterAvailableQuantity}"
            />
            <Input
                id="bookAvailableQuantity"
                type="Number"
                value="{viewModel>/newObject/availablequantity}"
                required="true"
                change=".onEnterInputValueChange"
                valueStateText="{i18n>bookAvailableQuantityValueState}"
            />
        </HBox>
        <beginButton>
            <Button
                text="{i18n>addRecord}"
                press=".onAddRecordJSONmodel"
            />
        </beginButton>
        <endButton>
            <Button
                text="{i18n>cancel}"
                press=".onCloseJSONModelAddRecordDialog"
            />
        </endButton>
    </Dialog>
</core:FragmentDefinition>